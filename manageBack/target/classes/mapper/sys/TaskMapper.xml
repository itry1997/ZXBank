<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.it.sys.mapper.TaskMapper">
    <select id="findAllTask" resultType="task">
        select x_task.* from x_task where userId = #{userId}
    </select>

    <update id="updateTask">
        update x_task
        set title = #{title},
            description = #{description},
            endtime = #{endtime},
            status = #{state}
        where id = #{id}
    </update>

    <select id="findTaskById" resultType="task">
        select x_task.* from x_task where id = #{id}
    </select>
</mapper>
